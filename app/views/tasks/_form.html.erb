<%= form_for(task) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <% if !t.object.new_record? %>
    <%= t.label "mark as done" %>
    <%= t.check_box :completed %>
  <% end %>
  <%= t.submit %>
<% end %>

